---
title: "Full Production Deployment Case Study"
slug: "production-case-study"
order: 2
difficulty: "expert"
duration: 20
tags: ["case-study", "production", "end-to-end", "deployment"]
objectives:
  - "Walk through a complete project built with Claude Code"
  - "See how every concept integrates in a real workflow"
  - "Understand the end-to-end development lifecycle"
  - "Apply lessons learned from production experience"
quiz:
  - id: "case-study-1"
    question: "What's the first thing to set up when starting a new project with Claude Code?"
    type: "multiple-choice"
    options:
      - "Write all the code first"
      - "CLAUDE.md — establish stack, conventions, and architecture rules before writing any code"
      - "Install MCP servers"
      - "Create agent teams"
    correct: 1
    explanation: "CLAUDE.md is always the first step. Before writing any code, establish your stack, conventions, and architecture rules. This ensures every Claude session from the very first follows consistent patterns. Adding CLAUDE.md later means earlier code was written without these standards."
  - id: "case-study-2"
    question: "In what order should you build your Claude Code infrastructure?"
    type: "multiple-choice"
    options:
      - "Agents → Settings → CLAUDE.md"
      - "CLAUDE.md → settings.json → commands → hooks → agents → skills"
      - "Settings → Agents → Commands"
      - "It doesn't matter"
    correct: 1
    explanation: "Build in layers: CLAUDE.md first (project context), then settings.json (permissions and environment), then commands (repeatable workflows), then hooks (automation), then agents (specialized roles), then skills (knowledge packages). Each layer builds on the previous ones."
  - id: "case-study-3"
    question: "When should you add custom agents to a project?"
    type: "multiple-choice"
    options:
      - "At the very beginning"
      - "When you find yourself repeatedly doing the same specialized task (e.g., security review every PR)"
      - "Never"
      - "Only for enterprise projects"
    correct: 1
    explanation: "Add agents when you identify recurring specialized tasks. If you're doing a security review on every PR, create a security-reviewer agent. If you're writing tests for every new service, create a test-writer agent. Don't create agents preemptively — create them when the pattern emerges."
  - id: "case-study-4"
    question: "What's the complete 4-phase development loop with Claude Code?"
    type: "multiple-choice"
    options:
      - "Write code -> push"
      - "Explore -> Plan -> Implement -> Commit, with /simplify between implement and commit"
      - "Ask Claude to do everything"
      - "Only use Claude for debugging"
    correct: 1
    explanation: "The 4-phase workflow: Explore (Claude reads relevant code, often in plan mode), Plan (propose the approach, press Ctrl+G to edit it), Implement (leave plan mode, write code against the plan), Commit (/commit with a message grounded in the plan). /simplify fits naturally between implement and commit as a quality pass."
---

## The Project: ScheduleForge API

Let's walk through building a complete project with Claude Code — from first commit to production deployment. This isn't theoretical. It's the workflow that experienced Claude Code users follow.

**The project**: ScheduleForge, a scheduling API for booking appointments. Node.js, TypeScript, Fastify, Prisma, PostgreSQL.

## Phase 1: Project Foundation

### Step 1: CLAUDE.md

The very first file in the project:

```markdown
# ScheduleForge — Appointment Scheduling API

REST API for managing appointments, availability, and scheduling.

## Stack
- Node.js 20 + TypeScript 5.4 (strict mode)
- Fastify 4.x + @fastify/autoload
- Prisma 5.x + PostgreSQL 16
- Zod for validation, Vitest for testing
- pnpm as package manager

## Architecture
- Routes → Handlers → Services → Repositories
- All business logic in services
- All database queries in repositories
- AppError for all errors (src/lib/errors.ts)
- Zod schemas for all input validation

## Conventions
- Functional style, no classes
- Named exports only
- kebab-case files, PascalCase types
- Explicit return types on async functions
- UTC for all date/time storage

## Commands
- pnpm dev — start dev server (port 3000)
- pnpm test — run Vitest
- pnpm lint — ESLint + Prettier
- pnpm db:migrate — run Prisma migrations
```

### Step 2: settings.json

```json
{
  "permissions": {
    "allow": [
      "Read", "Write(src/**)", "Write(tests/**)", "Write(prisma/**)",
      "Bash(pnpm *)", "Bash(git *)", "Bash(npx prisma *)", "Bash(npx vitest *)"
    ],
    "deny": [
      "Read(.env*)", "Write(package.json)", "Bash(rm -rf *)",
      "Bash(sudo *)", "Bash(curl *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      { "matcher": "Write(*.ts)", "command": "npx prettier --write $file" }
    ],
    "Notification": [
      { "command": "osascript -e 'display notification \"Claude needs you\" with title \"ScheduleForge\"'" }
    ]
  },
  "env": { "ENABLE_LSP_TOOL": "1" }
}
```

### Step 3: Project Scaffold

Follow the 4-phase workflow — Explore, Plan, Implement, Commit:

```bash
claude -n "project-scaffold" --permission-mode plan

"Plan the project structure for ScheduleForge. Include:
- Directory structure following our architecture
- Database schema (appointments, users, availability)
- Configuration setup"

# Review the plan. Ctrl+G to edit in your editor if needed.
# Shift+Tab to leave plan mode, then:

"Create the project scaffold following the plan"
```

## Phase 2: Core Development

### Step 4: Database Schema

```bash
claude -n "database-schema"

"Design and create the Prisma schema for:
- Users (id, email, name, role)
- Availability (userId, dayOfWeek, startTime, endTime)
- Appointments (id, userId, clientEmail, startTime, endTime, status)
Run the migration."
```

### Step 5: Core Features

Each feature gets its own session:

```bash
# Session: User management
claude -n "feature-users" --permission-mode plan
"Plan the user management module: CRUD operations, authentication"
# Review plan, Shift+Tab out of plan mode
"Implement the user module"
/simplify
/commit

# Session: Availability management
claude -n "feature-availability" --permission-mode plan
"Plan the availability module"
# Shift+Tab out of plan mode
"Implement it"
/simplify
/commit

# Session: Appointment booking
claude -n "feature-appointments" --permission-mode plan
"Plan the appointment booking module with conflict detection"
# Shift+Tab out of plan mode
"Implement it"
/simplify
/commit
```

One task, one session. Each session starts fresh with focused context and follows Explore -> Plan -> Implement -> Commit.

## Phase 3: Custom Commands

As patterns emerge, create commands:

```bash
mkdir -p .claude/commands
```

`.claude/commands/feature.md`:
```markdown
---
description: "Implement a new feature module"
argument-hint: "module-name"
---
Plan and implement the $ARGUMENTS module:
1. /plan — design the service, handlers, routes, and types
2. /execute — implement following CLAUDE.md conventions
3. Write tests for all service methods
4. /simplify — clean up
5. /commit with a descriptive message
```

`.claude/commands/test.md`:
```markdown
---
description: "Generate tests for a source file"
argument-hint: "file-path"
---
Generate comprehensive tests for $ARGUMENTS following our conventions.
```

## Phase 4: Agents and Skills

When recurring patterns appear:

### Security Reviewer (after the third PR review)

`.claude/agents/security-reviewer.md`:
```markdown
---
name: security-reviewer
description: Reviews code for auth bypass, input validation, data exposure, and injection
tools: Read, Grep, Glob, Bash(git diff:*)
model: claude-opus-4-6
permissionMode: plan
effort: high
---
Review code for: auth bypass, input validation, data exposure, injection.
Follow the checklist in our PR review conventions.
```

### API Endpoint Skill (after the third endpoint)

`.claude/skills/api-endpoint/SKILL.md`:
```markdown
---
name: api-endpoint
description: Creates new API endpoints following ScheduleForge conventions
---
[Templates for route, handler, service, repository, tests]
```

## Phase 5: MCP Integration

```bash
# GitHub for PR management
claude mcp add github -e GITHUB_TOKEN=ghp_xxx --transport stdio npx -y @modelcontextprotocol/server-github

# Database for schema inspection
claude mcp add postgres -e DATABASE_URL=$DATABASE_URL --transport stdio npx -y @modelcontextprotocol/server-postgres
```

## Phase 6: Hooks and Automation

### Auto-Format + Type Check

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write(*.ts)",
        "command": "npx prettier --write $file && npx tsc --noEmit 2>&1 | head -10 || true"
      }
    ]
  }
}
```

## Phase 7: Development Workflow

The daily workflow crystallizes:

```bash
# Morning: pick up a task — start in plan mode
claude -n "task-name" --permission-mode plan
"Plan the notification system for appointment reminders"

# Shift+Tab to leave plan mode, then implement
"Implement following the plan"

# Quality pass
/simplify
"Run the tests"

# Ship
/commit
"Create a PR targeting main"

# Review with the fresh-context reviewer pattern
"Use the security-reviewer subagent to review the changes in this PR"
```

## Phase 8: Testing and Deployment

### CI/CD Integration

```yaml
# .github/workflows/claude-review.yml
name: Claude Review
on:
  pull_request:
    branches: [main]
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install -g @anthropic-ai/claude-code
      - run: claude --permission-mode auto -p "Review this PR for security and quality" --output-format json --max-turns 15 --allowedTools "Read,Grep,Glob,Bash(git diff:*)"
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
```

### Pre-Deploy Checklist

`.claude/commands/deploy.md`:
```markdown
---
description: "Run deploy checklist"
argument-hint: "staging|production"
---
Deploy to $ARGUMENTS:
1. Verify branch: !`git branch --show-current`
2. Verify tests: !`pnpm test --run 2>&1 | tail -5`
3. Verify build: `pnpm build`
4. Tag if production
5. Push and deploy
```

## The Complete Toolkit Used

| Phase | Tools Used |
|---|---|
| Foundation | CLAUDE.md, settings.json |
| Development | Plan mode (Shift+Tab, Ctrl+G), /simplify, /commit |
| Automation | Commands, hooks, agents |
| Integration | MCP (GitHub, Postgres) |
| Quality | Security reviewer agent, /simplify, Writer+Reviewer pattern |
| Deployment | Deploy command, CI/CD with `--permission-mode auto` + `--allowedTools` |
| Monitoring | /cost, /insights |

## Key Takeaway

A production project with Claude Code follows a clear progression: CLAUDE.md first (always), then settings.json, then iterative feature development with the 4-phase Explore -> Plan -> Implement -> Commit loop (plus /simplify as the quality pass). Commands emerge from repeated patterns, agents from recurring specialized roles, and skills from reusable knowledge. MCP connects external tools, hooks automate quality gates, and CI/CD extends Claude into your pipeline. The result: a codebase built consistently with every Claude Code feature working in concert.
